# Used only by scripts/chart-test.sh for standalone helm lint/template on the
# node-data-broker subchart. The parent topograph chart supplies `global` at install time.
global:
  provider:
    name: test
